# Wazuh dashboard -> manager API config — TEMPLATE ONLY.
# The @API_PASSWORD@ placeholder is substituted host-side by wazuh-secrets.service
# (see ../../../../modules/wazuh.nix) from /var/lib/mgmt-secrets/wazuh.env; the
# rendered file is mounted from /var/lib/mgmt-secrets/wazuh/wazuh.yml.
hosts:
  - 1513629884013:
      url: "https://wazuh.manager"
      port: 55000
      username: wazuh-wui
      password: "@API_PASSWORD@"
      run_as: true
